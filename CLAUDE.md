# SATELLITES — 開発の引き継ぎ

盤面を星空に見立てて音源の星を置き、核を中心に周回させて放置するアンビエント生成ツール。
ブラウザだけで動き、GitHub Pages に静的ホストする。

- 公開 URL: https://warpshoot.github.io/satellites/
- もとは warpshoot/lab の中で DRIFT という名前で作り、改名したうえで独立させた

## 成り立ち

もとは DRIFT という名前で、SPEC（チャットで提示、リポジトリには無い）の Phase 1 として作った。
そのあと「星が核を回る」という見立てに寄せる過程で、**足したものより捨てたものが多い**。
捨てた理由が分からないと親切心で復活させてしまうので、下の「捨てたもの」を先に読むこと。

## 技術構成

- 素の HTML / CSS / JavaScript（ES Modules）。**ビルドステップなし**。そのまま置いて動くこと
- 外部ライブラリなし。Web Audio API のみ
- AudioWorklet は使わない。GRAIN は 25ms 間隔・100ms 先読みの lookahead scheduler で足りている

```
index.html
css/style.css
js/main.js                  エントリ。状態・位置の解決・カメラ・配線
js/presets.js               プリセット（形式はパッチと同じ）
js/state.js                 パッチ、localStorage、パラメータ定義
js/patchio.js               パッチの持ち出しと持ち込み（ファイル・リンク・クリップボード）
js/audio/engine.js          AudioContext、マスターバス、リバーブ、ピンポンディレイ、リミッタ
js/audio/ir.js              リバーブ用インパルス応答の手続き生成
js/audio/music.js           基音と音階、音程の吸着
js/audio/noiseBuffer.js     共有ホワイトノイズ
js/audio/voices/base.js     Voice の共通インターフェースと距離の式
js/audio/voices/{drone,noise,grain,drive,bell,pluck}.js
js/audio/voices/registry.js 音源種別の配列
js/ui/field.js              盤面。投影・カメラ・星の描画・軌道の描画
js/ui/looks.js              見た目（A〜F）と背景の星
js/ui/strip.js              星の一覧
js/ui/panel.js              パネルとコントロールの組み立て
js/ui/master.js             マスターのパネル
```

## 中心の考え方

**核は盤面のど真ん中に固定**されていて動かない。星はそこを周回するか、止まっているか。

音は**核からの3次元距離だけ**で決まる。視点からの距離という概念は無い。

| 何が | 何を決める |
|---|---|
| 核からの距離 | ドライの減り方（逆二乗）、ウェットとドライの比、空気の吸収、星の大きさ |
| 横位置 | パン |
| 縦位置 | 音色の傾き（低域と高域を逆向きに動かすシェルフ） |
| 面からの浮き（軌道傾斜で生まれる z） | にじみ（送りを増やし直接音を削る、送りだけのプリディレイ）と前後（高域） |
| 音量 | その星自身の音量。距離とは別に掛かる |
| 明るさ（共通パラメータ） | 縦位置の傾きに足すオフセット。位置を変えずに明暗だけ動かす |

**距離はウェットとドライの比で表す。** 送りを音量段の後ろから取ると、遠い星は
ウェットごと小さくなって、ただ消えるのと同じになる。送りは `levelGain` の前から
分岐し、`wetFromNear()` でドライよりゆっくり落とす。実測で、近い星のウェット/ドライ比が
0.40、遠い星（near=0.2）が 3.88。遠ざけると音量ではなく「風呂場に入る」ほうへ寄る。

**音程は全体で1組。** 基音と音階はマスターに置き、鳴らす直前に `quantize()` で
一番近い音度へ寄せる（DRONE と DRIVE の基音、GRAIN / BELL / PLUCK の1発ごとの音程）。
デチューンは吸着させない。潰すと重ねる意味が消える。音階「なし」で素通しにできる。

**転調はエンジンが持ち、パッチには焼かない。** マスターの「転調」は速さだけで、
いま何半音ずれているか（`engine.tuning.offset`）は保存しない。焼くと、開くたびに
違う調で始まって「保存した音」でなくなる。行き先は `keySteps()` が音階の音度から
作る候補（上下7半音まで）。半音の自由歩行にすると、数分ごとに関係のない調へ飛んで
「別の曲が始まった」に聞こえる。移るときは時定数 4 秒で滑らせる。

**位置は明暗と距離を同時に決めてしまう。** だから「遠いのに明るい」が位置だけでは
作れない。共通パラメータの「明るさ」がそこを埋める。これは距離のノブではなく
音色のノブなので、捨てた「近さのパラメータ」とは別物（あれは、どこに置くかが
距離なのに別のノブを置くから嘘になった）。

**面からの浮きは距離とは別口。** `Voice.setElevation(z)` が受ける。`|z|` でにじませ
（直接音 -35%、送り +90%、送りだけ 0〜30ms のプリディレイ）、符号で前後を分ける
（奥は高域 -4.5dB、手前は +1.5dB）。傾斜90度・正円を1周させた実測で、
ドライは 0.5dB しか動かないのにウェットが 3.7dB、高域シェルフが 3.6dB 動く。
音量ではなく「にじみ方」で動きを出すので、周回しても土台が波打たない。

**ゆらぎは共通パラメータ。** 周期の噛み合わない正弦を重ねて、戻ってこない揺れを作る
（`Voice.driftAt()`）。空気の吸収・定位は共通で、デチューン量・帯域中心・粒の間隔・
歪みの深さは種別ごとに `applyDrift()` で受ける。放置して聞くものなので、
これが無いと数分で状態が止まって聞こえる。

**一番遅い成分を10分に取る。** 37〜97秒の重ねだけだと、3〜4分で「もう一周分の
景色は見た」になる。`SLOW_RATIO` で各成分にもう1オクターブ以上遅い正弦を足してある。

**軌道は発音の時刻も決められる。** `Voice.setOrbitClock()` に位相・時刻・角速度を
渡し、`orbitTicks()` が先読みの窓の中で刻み目を通過する時刻を返す。刻みの原点は
近点なので、つぶれた軌道なら一番近づいた瞬間に鳴る。GRAIN / BELL / PLUCK の
「タイミング」を軌道にすると、間隔ではなくこちらが鳴らす。
**これが無いと、見えている動きと聞こえるイベントが噛み合わない**
（軌道が触るのは距離・パン・傾き・にじみという、なめらかな量だけだった）。
周期を割り切れない値にした星を並べると、打点が揃うのは何時間かに一度になる。
間隔のばらつきは乱数なので、これは `jitter` では作れない。

- 周回の半径・位相はパラメータ。**周回中の星は指では動かせない**（`field.js` の
  pointermove が `v.orbit` で早期 return する）。位置は軌道が決めるという筋。
  そのため `app.moveTo()` の周回側の枝は誰も呼ばない死んだコードになっている
- 正円の軌道は距離が変わらないので音量も一定。**つぶれた軌道だけが呼吸する**（近日点で大きく鳴る）
- 軌道傾斜角で面を奥へ倒せる。**ただし円軌道は、倒しても核からの距離が変わらない**。
  回転させているだけなので当然で、実測でも傾斜45度・正円の音量振幅は 0.9dB しかなかった。
  距離だけに頼ると、傾斜は目に見えて耳に聞こえない操作になる
- 距離に床は無い。盤面の外へ出ればそのまま減り続ける

## 捨てたもの（復活させないこと）

- **錨（星どうしの親子関係）**。どの星も中心を選べる仕組みを作ったが、中心を1つに固定したら
  輪の検出も再帰の位置計算も結びの線も全部不要になった
- **音を持たない星（STAR 種別）**。中心が固定なら置く意味がない
- **一点透視**。核と消失点が同じ場所で衝突して「近さ」の意味が壊れた。真上から見た図にしてある
- **「近さ」のパラメータと奥行きのギズモ**。どこに置くかが距離なので、別のノブは嘘になる
- **軌道の種類（ふらつき／振り子／呼吸）**。振り子は傾斜90度、呼吸はつぶれた軌道と同じもの。
  ふらつきは衛星ではない
- **ダブルタップで周回を切り替えるジェスチャ**。盤面に見えない操作を置かない
- **長押しで削除と、盤面外へ投げて削除**。同じ理由。どちらも盤面に手がかりが無く、
  作った本人が存在を忘れるくらい誰にも見つからなかった。削除はパネルの「削除」が
  1つだけ持つ。星への指の操作は、選ぶことと動かすことだけ。
  確認バブル（`ask`）自体は残してあり、パネルの削除がそれを出す
- **星の発光**。光るのは核だけ。周りは光を受けている側
- **疑似的な球体グラデーション**。半径 2〜11px では陰影が載らない。平面と輪郭の記号にしてある
- **即配置モードと Undo**。スコープから外すと決めた
- **演奏レイヤー（盤面の下の帯）**。触っている間だけ効いて離すと戻る倍率を、
  リボンで振る仕組み。時間・引力・灼くの3本まで作って、全部畳んだ。
  - 時間と引力は**既にあるものを一様に拡大縮小するだけ**だった。速いか遅いか、
    大きいか小さいかしか起きず、振っても音の性格が変わらない
  - 灼きは音を作り替えていたので価値はあったが、**「演奏」という枠がこのアプリに
    合わなかった**。置いて放っておくものに、掴み続けないと効かない操作を足すと、
    使う側の姿勢が噛み合わない。灼きはマスターの普通のノブに移した
  - 残骸は残していない。倍率を積む時計、引力の座標変換、潮汐、畳む UI、全部消した。
    もし似たものを作りたくなったら、まず「掴み続ける価値があるか」を先に決めること

## 画面の言葉と並び

**世界観の言葉とノブの名前は別のレイヤー。** 核・星・周回・軌道はこのアプリの
見立てそのものなので画面に残す。いっぽうパラメータ名は**DTM の共通語**に寄せる。
「灼き」はサチュレーション、「ゆらぎ」はドリフト、「サイコロ」はランダム。
自作の呼び名は、覚えるコストを払わせたぶん何も返さない。

**機能を足すたびに、ここがずれる。** 実際、星ごとのチップを「ランダム」に直した
あとでマスターに「サイコロ／全部振る」が生え、`width` は3種別で「ステレオ幅」、
2種別で「定位のばらつき」になっていた。**新しいノブを足すときは、まず下の表に
同じ現象が載っていないか見る。** 載っていたらその語を使う。載っていなければ
足す。表に無い語を画面に出さない。

| 現象 | 画面の語 | 内部キー |
|---|---|---|
| オシレータの音程 | ピッチ | `freq` / `center` |
| フィルタの中心周波数 | 周波数 | `center`（NOISE） |
| 音程の散らばり | ピッチ幅 | `spread` |
| 左右の広がり | ステレオ幅 | `width` |
| 発音の間隔 | 間隔 | `interval` |
| 間隔の乱れ | ばらつき | `jitter` |
| 1周を何回に割るか | 分割 | `hits` |
| 発音を何に従わせるか | タイミング（フリー／軌道） | `trigger` |
| 戻ってこない遅い揺れ | ドリフト | `drift` / `delay.wow` |
| 振幅の周期的な揺れ | トレモロ深さ／速度 | `swellDepth` / `swellRate`（NOISE） |
| 音程の周期的な揺れ | ビブラート深さ／速度 | `swellDepth` / `swellRate`（DRIVE） |
| 波形を潰す量 | ドライブ（星）／サチュレーション（マスター） | `drive` / `burn` |
| 高域の傾き | 明るさ（星）／トーン（ディレイ） | `tone` / `delay.tone` |
| 音を全部振り直す | ランダム | `randomize` / `randomizeAll` |

- **ラベルに説明を入れない。** 「つぶれ具合（0 = 正円）」は離心率。括弧の中身は
  スライダを動かせば分かる。ラベルは名詞1語で、行の高さを1行に保つ
- **同じ語を2つの意味で使わない。** マスターの「基音」（キーのルート）と
  DRONE/DRIVE の「基音」（オシレータ周波数）は別物だったので、
  **ルート** と **ピッチ** に割った
- **意味が食い合う2本は、食い合わない名前にする。** リバーブの「長さ」と「減衰」は
  どちらも尾の長さの話に読めて、長さ 15 秒・減衰 6 だと何も伸びない。
  前者は部屋の寸法、後者は尾の吸われ方なので **サイズ** と **吸収** に割った
- **画面に出ていない事実を作らない。** 転調を入れると、選んだルートと鳴っている
  ルートがずれる。ずれたまま出口が無いと、キーの欄が嘘をつく。セクションに
  「いま ○」を1行出して、1秒ごとに引き直す。ディレイのタイムも同じで、
  「軌道に同期」が ON のあいだは一番速い周回から時間が決まるので、ノブの値は
  使われない。**寝かせるだけでは足りない**（寝ていても数字は見えている）。
  行を `disabled` にしたうえで「いま ○ms」を出す。実際、プリセットが
  180ms と書いたまま 875ms で鳴っていた
- **音程のノブには鳴る音名を添える。** 吸着があるので、ノブの Hz と実際に鳴る音は
  一致しない。`note: true` を付けると `buildControl` が `quantize()` 済みの音名を出す
- **違う現象に同じ語を当てない。** NOISE の swell は振幅（トレモロ）、
  DRIVE の swell は音程（ビブラート）。内部キーが同じでもラベルは分ける
- **セクション見出しが文脈を持つ。** 「リバーブ」の中の行は「長さ」でいい。
  「リバーブ長さ」と書くと見出しが二度手間になる
- **記号に意味を持たせない。** 見た目 A〜F は形そのもの（`.star-mark`）を
  並べる選択肢に替えた。押すまで結果が分からない札は札として失格
- トーストは成功が `〜した`、失敗が `〜ない` で揃える

**音色パネルの並びは信号の流れ。** 音源 → 音色（種別固有）→ エンベロープ →
動き → ミックス。よく触る種別固有のパラメータを最下段に置かない。
束は `base.js` の `ENV_PARAMS` / `MOTION_PARAMS` / `MIX_PARAMS`、
マスターは `state.js` の `MASTER_GROUPS` が持つ。**順番を画面側に書かない。**

**周回 OFF でも軌道の行は消さない。** `disabled` にして残す。消すと押すたびに
パネルの高さが跳ねるし、何が隠れているかも分からない。

**条件付きのパラメータも行を消さない。** パラメータ定義に `when(v)` を足すと、
偽のとき `disabled` になる（周回の行と同じ扱い）。消すと押すたびに高さが跳ねる。

**スライダはダブルタップで既定値に戻る。** パラメータ定義に `def` を足すだけで
効く（`buildControl` が拾う）。戻せないノブは、試しに動かすこと自体が怖くなる。

## パラメータの刻み方

`scale` は4つ。`lin` / `log` / `int` / `pow`。

**0 から始まるノブは `pow`。** log は 0 を通せず、lin だとスライダの左半分が死ぬ。
デチューン 2cent と 5cent はうなりの速さが倍以上違うのに、0〜50 の lin では
1.5mm しか離れていない。`pow`（既定 γ=2）は 0 を出せて、下を細かく上をざっくり刻む。

対象: デチューン / ドライブ / トレモロ深さ / ビブラート深さ / ピッチ幅 / ばらつき /
ドリフト（星とディレイの両方）/ リバーブ / ディレイ / 離心率 / フィードバック /
モジュレーション / 転調

ランダム（`app.randomize` / `app.randomizeAll`）は必ずこの刻み方に沿って振る。log のノブを lin で
振ると毎回上限付近に寄って、何度振っても同じ音しか出てこない。
振るのは音そのものだけ。置いた場所と周回には触らない。アタックとリリースも
振らない（20秒のアタックを引くと「押したのに何も起きない」になる）。

## ゲインステージ

```
各ボイス → masterBus(0.45) → 30Hz ハイパス → 灼き段 → リミッタ → マスター音量 → 出力
```

**灼き段はリミッタの手前。** 暴れても頭は押さえられる。ドライと混ぜる作りには
しない。WaveShaper は 4x オーバーサンプルでわずかに遅れるので、素の音と足すと
端末のモノスピーカーで櫛状に穴が開く（ボイスの浮きと同じ話）。代わりに
**曲線そのものを「効かないときは y = x」**にしてある。膝 k を超えたところからだけ
tanh で寝かせ、k = 1 なら完全に素通し。

膝は平方根で、突っ込む量は線形で降ろす。両方を線形にすると帯の左半分が
素通しのままで、右の端に来てからいきなり潰れる。実測の THD（入力 0.45）で
0 → 3.4 → 18.9 → 26.5 → 31.0 → 34.5 → 36.5%。実機では 2kHz 以上の
エネルギー比が -11.9dB → -7.2dB に増える。

基準の高さ（0.3）の通り方だけは前後で揃える。揃えないと歪みのノブが
音量のノブになる。曲線は 24 段に丸めてキャッシュし、段が変わったときだけ
差し替える。突っ込む量と戻す量はランプで動かす。

深さはマスターの「サチュレーション」0〜1（内部名は `burn`）。パッチに入るので保存もリンクも通る。
旧いパッチには無いので 0（＝素通し）に落ちる。

**種別ごとの素の出音は揃える。** DRIVE は WaveShaper を通したぶん波形が矩形へ
寄って RMS が上がり、補正前は他より 4.6〜7.3dB 大きかった。1つ置くと他の星が
全部引っ込んで聞こえる。しかも歪み量を上げるほど大きくなるので、あのノブは
半分が音量ノブだった（実測で歪み 0 → 1 の間に 12dB）。

のこぎり波は `[-1, 1]` を一様に舐めるので、**曲線を同じ範囲で平均すれば
通過後の RMS が出る**（`curveRms()`）。これで割って DRONE に揃えてある。
実測と ±0.85dB で一致し、揃えたあとの振れ幅は歪み 0〜1 で 1.1dB。

測るときは**ゆらぎを 0 にする**こと。ゆらぎを入れたまま測ると同じ設定でも
読みが 6dB 動いて、補正が効いていないように見える。

- **ヘッドルームはリミッタの手前で確保する。** 素通しで8点集めると常時 7dB 潰れて、
  粒が落ちるたびに土台が凹む。実測で、トリム込みなら 8点でも -1.8〜-3.0dB に収まる。
  上限を 12 点へ上げたぶん masterBus を 0.5 → 0.45 に下げた。12 点を雑に置いた
  実測でリミッタの削減は 0.4dB 以下、ピーク -2.1dBFS
- **マスター音量はリミッタの後ろ。** 前に置くと、音量を絞るほど潰れ方まで変わる
- 30Hz 以下は端末では鳴らないのにヘッドルームだけ食う。先に捨てる

## 踏むと痛い箇所

1. **`confirm()` を使わない。** iOS はシステムダイアログが出ると音声セッションを持っていき、
   AudioContext が中断される。確認は盤面の中に出す
2. **`resume()` はユーザー操作の中でしか通らない。** `visibilitychange` から呼んでも弾かれる。
   復帰は三重（statechange でオーバーレイ／visibilitychange／画面のどこかを触る）
3. **意図的な停止は自動復帰の対象外にする。** これをやらないと停止ボタンを押した指で鳴り出す
4. **AudioParam の直接代入はクリック音になる。** 変更は `engine.ramp()`（setTargetAtTime）経由
5. **ボイス削除は release をかけてから disconnect**。即切りはプツッと鳴る
6. **リバーブの IR 再生成は音が途切れる。** ノブのドラッグ中は生成せず、離したときに一度だけ
7. **ノイズバッファは全ボイスで共有**。ボイスごとに作らない
8. **復帰時に GRAIN のスケジューラをリセット**。過去時刻に予約して暴発する
9. **DynamicsCompressor を省略しない。** ドローン複数本で容易にクリップする
10. **WaveShaper は `oversample: '4x'` 必須**。ディレイの feedback は 0.85 を超えさせない
11. **送りは `levelGain` より前から分岐する。** 後ろに回すと距離の表現が消える
12. **ドライを片側だけ遅らせない。** 浮きのプリディレイは送りにだけ掛ける。
    ドライを左右でずらすと、端末のモノスピーカーで合わさったときに櫛状に穴が開く
13. **ディレイの帰還フィルタは左右別々にする。** 1本に集めると帰還がモノに潰れる。
    襷掛けなので一周の利得は片側の2乗。`setDelayFeedback()` で √ を掛けて戻してある
14. **GRAIN は密度で音量が変わらないようにする。** 間隔 0.05s × 粒 2000ms で 40粒重なる。
    `1/√(重なり数)` で割る。粒の窓は余弦。直線の三角だと短い粒で角が鳴る
15. **音源ピッカーの幅は CSS で固定する。** `max-width` にして成り行きの幅に
    すると、絶対配置の箱は `left` を動かすたびに「残りの幅」で折り返し直される。
    寄せる前に測った寸法が当てにならなくなり、右端では 220x76 のつもりが
    116x178 に化けて盤面の上へはみ出していた
16. **ピッカーは停止ボタンの下から逃がす。** 停止ボタンは盤面の左上に居座って
    いてピッカーより手前に描かれる。手前に出すのではなく下へずらす。
    塞ぐ側を入れ替えるだけでは、今度は停止が押せなくなる
17. **見た目と当たり判定を分ける。** 星は小さく描くが、掴める範囲は 46px 確保してある
18. **リンクは `hashchange` でも拾う。** 開いたままのタブに同じページのリンクを
    流し込まれても再読み込みは起きない。起動時に1回読むだけだと何も起きずに黙る
19. **読み込んだらハッシュを消す。** 残すと、編集したあとの再読み込みでリンクの
    内容に巻き戻る
20. **灼きの曲線は amount 0 で厳密に y = x にする。** 「ほとんど素通し」では
    駄目で、既定の状態で音が変わってしまう。膝 k = 1 で恒等になる形にしてある
21. **GRAIN の粒の形は二乗平均で正規化する。** 弾く形は平らな部分が無いぶん
    痩せるので、そのままだと形のノブが音量のノブになる。`windowRms()` で
    もとの対称な窓に合わせてある（検算でズレ 0）
22. **位置を `[0,1]` に丸めない。** 丸めると盤面の外で距離が頭打ちになり、音の変化が止まる
23. **PLUCK の正規化は「頭 0.25 秒の二乗平均」で取る。** ピークで揃えると波高率の
    ぶんだけ胴が痩せ（実測でピッチ 55Hz と 880Hz の二乗平均が 11.5dB ずれた）、
    全長の二乗平均で揃えると減衰の長さがそのまま読みに乗って、減衰が音量ノブになる。
    ピーク上限は 1 より上（1.35）に置く。1 未満にすると上限が常に効いて、
    結局ピークで揃えたのと同じになる。揃えたあとの振れ幅はノブ9通りで 2.0dB、
    BELL とのピーク差は 0.8dB
24. **KS の励起は角を取る。** 生の白ノイズを入れると頭の一撃だけが突出する。
    1極ローパスを輪にして2周かける（1周だと継ぎ目の段が周期ごとのクリックになる）
25. **軌道の刻み目は重複して拾わない。** 先読みの窓は 25ms ごとに 100ms 先まで見るので
    大きく重なる。`_lastTick` より後の刻みだけを鳴らす
26. **同じことを2箇所に書かない。** `Engine.applyMaster()` は `main.js` 側の同名
    メソッドと二重になっていて、誰も呼ばない死んだコードだった。ディレイのトーンを
    足したとき死んでいるほうだけ直して、プリセットのトーンが効かなかった。消してある

## 制約

- 同時発音 12点（`MAX_VOICES`）。埋まったら新規配置を断る。古い星の自動削除はしない。
  枠を増やしたのは厚く重ねるためではなく、役割を1つずつ分けられるようにするため。
  実際に呼吸するのは 4〜6点のまま
- 衝突判定は一切ない。星どうしは素通りし、軌道が交差しても何も起きない
- パッチは version 3。`localStorage` に debounce 500ms で保存。旧 `drift.patch.v1` から引き継ぐ
- ソロとミュートは保存しない。無音で復帰すると壊れたように見えるため
- **ソロ中に鳴るのは1つだけで、鳴るのは選んでいる星。** 星を選び直すとソロも
  そっちへ移る。状態として持っているのは `soloOn` の真偽値ひとつだけで、
  対象は選択から引く（`soloTargetId()`）。「ソロ中の星」を別に覚えると選択との
  二重管理になって必ずずれる。選択を外している間は最後に見ていた星。
  それも消えていたらソロ自体を降ろす（どれも鳴らないのに札だけ出るのを避ける）
- 起動時のボイスは一斉に立ち上げない。散らして入れる（揃うと「全部同時に開いた」と聞こえる）

## パッチの持ち出しと持ち込み

保存の実体は `localStorage` の1枠だけ。名前を付けて複数残す仕組みは持たない。
代わりに、その1枠を外へ出す口を3つ用意してある（マスターの「パッチ」）。

- **ファイル** `savePatchFile()` は Blob と `<a download>`。整形して書き出すので手で直せる。
  File System Access API は iOS に無いので使わない。読み込みは `<input type="file">`
- **リンク** `#p=<タグ><base64url>`。タグ `d` は deflate-raw、`r` は生の JSON。
  `CompressionStream` が無い環境では自動で `r` に落ちる。実測で8点のパッチが
  JSON 3239 バイト → リンク 926 文字（圧縮無しだと 4349 文字）
- **クリップボード** `navigator.clipboard` → `execCommand('copy')` → それも駄目なら
  パネルの中に textarea を出して手で選ばせる。3段構えにしないと環境差で黙って落ちる

持ち込みは全部 `sanitize()` を通す。読めない文字列は「読めなかった」と出すだけで、
いまの配置には触らない。上書きする前に `backupCurrent()` で1つ前を退避し、
「前の配置に戻す」を出す（戻したら退避は消す。2つ前には戻れない）。

## AI にパッチを作らせる

`docs/PATCH_AUTHORING.md` が、そのための1枚。添付して「これを読んでパッチを作って」と
渡すと JSON が返る。それをマスターの「貼り付けを読む」に流す。

**このリポジトリを読ませない前提**なので、値域も音階の中身も転記してある。
パラメータの min/max を変えたら、このファイルも直すこと（直さないと静かに嘘になる）。

## 確かめ方

推測で書かず、**実際に鳴らして測る**こと。ヘッドレス Chromium が使える。

```js
// Playwright は /opt/node22/lib/node_modules/playwright
// 盤面を操作したあと、モジュールを import して中を読む
await page.evaluate(async () => {
  const { engine } = await import('/js/audio/engine.js');
  const v = engine.voices[0];
  return { gain: v.levelGain.gain.value, send: v.reverbSend.gain.value };
});
```

- 音量やリバーブは AnalyserNode か AudioParam の値を直接読む
- **出音を測るなら `masterGain` の後ろに挿す。** `limiter` から取るとマスター音量が
  効いていない値を読むので、音量を動かしても読みが変わらない（配置ごとのトリムを
  当てても、測った数字が動かないまま何度も振り直すことになる）
- L/R の相関を測ればステレオの広がりが分かる
- リミッタ後のピークを測ればクリップの有無が分かる
- `page.on('pageerror')` を必ず張る。UI の不具合はたいてい例外が原因

注意: Playwright の `hasText` は部分一致なので、`'ON'` は `'DRONE'` にも当たる。

## プリセット

`js/presets.js` に5つ。形式は保存・リンクと同じ version 3 なので、読み込みは
`sanitize()` の同じ関門を通る。上書きの前に `backupCurrent()` を取るので、
押し間違えても「前の配置に戻す」で帰ってこられる。

音作りの知識が無い人にも、少し DTM をやる人にも、一番速く伝わるのは
「完成したものを先に鳴らして、あとから分解させる」こと。説明より速い。

**5つで示すのは「色々作れる」ことそのもの。だから性格の軸をずらして選ぶ。**

| | 何を示す |
|---|---|
| 合唱 | 止まった和音・12点の分業・長調 |
| 白昼 | 高い音域・低音なし・リディア |
| 砂 | 粒の密度が音色になる・吸着なし |
| 点呼 | 正確な反復・短い減衰・単音 |
| 時計 | 軌道同期の発音・転調・ディレイのドリフト・明るさオフセット |

一度、5つとも作り直している。前の5つは、どれも「止まった DRONE の土台 +
NOISE の質感 + 主役1〜2」の 4〜5 点で、**ノブの下半分をひとつも使っていなかった**。
アタックは全部3秒以上（log のスライダで下 75% が空白）、鐘と弦の減衰は4秒以上（下
81〜90%）、ばらつきは 25% 以上、ステレオ幅は 0.7 以上、音階は13種のうち5種で
全部が短調系か無調。音色の散り方（スペクトル重心 1730〜3243Hz）は足りていたのに、
速さ・起伏・調の明暗が揃っていた。**同じ曲を5回弾いている状態**だった。

**使用域はスライダ上の位置で測る。** log のノブを値の幅で測ると空白の大きさを
見誤る（アタック 3〜20 秒は、値では 85% 使っているように見えて、スライダでは 25%）。

**新しい仕掛けを入れたら、それを使うプリセットを1つ持たせる。** ただし枠は5つ
なので、足すのではなく**どれかの軸に載せる**。前は足し続けた結果「時計」が
軌道同期・転調・ディレイのドリフト・明るさオフセットを全部ひとりで背負って、
残りの4つが旧世代のまま取り残された。

**5つの音量は揃える。** 押し比べる道具なので、鳴る大きさが違うと一番大きいものが
一番良い音に聞こえる。作り直す前は 90 秒平均で 6.5dB 開いていた。

## 残っている宿題

- Phase 2: マイク入力、グラニュラー音源、MediaRecorder による録音。
  全ボイスが masterBus に集まる構造は守ってあるので、録音は数十行で入る
