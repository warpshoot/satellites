# SATELLITES — 開発の引き継ぎ

盤面を星空に見立てて音源の星を置き、核を中心に周回させて放置するアンビエント生成ツール。
ブラウザだけで動き、GitHub Pages に静的ホストする。

- 公開 URL: https://warpshoot.github.io/satellites/
- もとは warpshoot/lab の中で DRIFT という名前で作り、改名したうえで独立させた

## 成り立ち

もとは DRIFT という名前で、SPEC（チャットで提示、リポジトリには無い）の Phase 1 として作った。
そのあと「星が核を回る」という見立てに寄せる過程で、**足したものより捨てたものが多い**。
捨てた理由が分からないと親切心で復活させてしまうので、下の「捨てたもの」を先に読むこと。

## 技術構成

- 素の HTML / CSS / JavaScript（ES Modules）。**ビルドステップなし**。そのまま置いて動くこと
- 外部ライブラリなし。Web Audio API のみ
- AudioWorklet は使わない。GRAIN は 25ms 間隔・100ms 先読みの lookahead scheduler で足りている

```
index.html
css/style.css
js/main.js                  エントリ。状態・位置の解決・カメラ・配線
js/state.js                 パッチ、localStorage、パラメータ定義
js/audio/engine.js          AudioContext、マスターバス、リバーブ、ピンポンディレイ、リミッタ
js/audio/ir.js              リバーブ用インパルス応答の手続き生成
js/audio/music.js           基音と音階、音程の吸着
js/audio/noiseBuffer.js     共有ホワイトノイズ
js/audio/voices/base.js     Voice の共通インターフェースと距離の式
js/audio/voices/{drone,noise,grain,drive}.js
js/audio/voices/registry.js 音源種別の配列
js/ui/field.js              盤面。投影・カメラ・星の描画・軌道の描画
js/ui/looks.js              見た目（A〜F）と背景の星
js/ui/strip.js              星の一覧
js/ui/panel.js              パネルとコントロールの組み立て
js/ui/master.js             マスターのパネル
```

## 中心の考え方

**核は盤面のど真ん中に固定**されていて動かない。星はそこを周回するか、止まっているか。

音は**核からの3次元距離だけ**で決まる。視点からの距離という概念は無い。

| 何が | 何を決める |
|---|---|
| 核からの距離 | ドライの減り方（逆二乗）、ウェットとドライの比、空気の吸収、星の大きさ |
| 横位置 | パン |
| 縦位置 | 音色の傾き（低域と高域を逆向きに動かすシェルフ） |
| 音量 | その星自身の音量。距離とは別に掛かる |

**距離はウェットとドライの比で表す。** 送りを音量段の後ろから取ると、遠い星は
ウェットごと小さくなって、ただ消えるのと同じになる。送りは `levelGain` の前から
分岐し、`wetFromNear()` でドライよりゆっくり落とす。実測で、近い星のウェット/ドライ比が
0.40、遠い星（near=0.2）が 3.88。遠ざけると音量ではなく「風呂場に入る」ほうへ寄る。

**音程は全体で1組。** 基音と音階はマスターに置き、鳴らす直前に `quantize()` で
一番近い音度へ寄せる（DRONE と DRIVE の基音、GRAIN の粒ごとの音程）。
デチューンは吸着させない。潰すと重ねる意味が消える。音階「なし」で素通しにできる。

**ゆらぎは共通パラメータ。** 周期の噛み合わない正弦を重ねて、戻ってこない揺れを作る
（`Voice.driftAt()`）。空気の吸収・定位は共通で、デチューン量・帯域中心・粒の間隔・
歪みの深さは種別ごとに `applyDrift()` で受ける。放置して聞くものなので、
これが無いと数分で状態が止まって聞こえる。

- 周回の半径・位相はパラメータ。ドラッグでも書き換わるが、**入口が2つあるだけで値はひとつ**
- 正円の軌道は距離が変わらないので音量も一定。**つぶれた軌道だけが呼吸する**（近日点で大きく鳴る）
- 軌道傾斜角で面を奥へ倒せる。倒すと距離が周期的に変わる
- 距離に床は無い。盤面の外へ出ればそのまま減り続ける

## 捨てたもの（復活させないこと）

- **錨（星どうしの親子関係）**。どの星も中心を選べる仕組みを作ったが、中心を1つに固定したら
  輪の検出も再帰の位置計算も結びの線も全部不要になった
- **音を持たない星（STAR 種別）**。中心が固定なら置く意味がない
- **一点透視**。核と消失点が同じ場所で衝突して「近さ」の意味が壊れた。真上から見た図にしてある
- **「近さ」のパラメータと奥行きのギズモ**。どこに置くかが距離なので、別のノブは嘘になる
- **軌道の種類（ふらつき／振り子／呼吸）**。振り子は傾斜90度、呼吸はつぶれた軌道と同じもの。
  ふらつきは衛星ではない
- **ダブルタップで周回を切り替えるジェスチャ**。盤面に見えない操作を置かない
- **星の発光**。光るのは核だけ。周りは光を受けている側
- **疑似的な球体グラデーション**。半径 2〜11px では陰影が載らない。平面と輪郭の記号にしてある
- **即配置モードと Undo**。スコープから外すと決めた

## ゲインステージ

```
各ボイス → masterBus(0.5) → 30Hz ハイパス → リミッタ → マスター音量 → 出力
```

- **ヘッドルームはリミッタの手前で確保する。** 素通しで8点集めると常時 7dB 潰れて、
  粒が落ちるたびに土台が凹む。実測で、トリム込みなら 8点でも -1.8〜-3.0dB に収まる
- **マスター音量はリミッタの後ろ。** 前に置くと、音量を絞るほど潰れ方まで変わる
- 30Hz 以下は端末では鳴らないのにヘッドルームだけ食う。先に捨てる

## 踏むと痛い箇所

1. **`confirm()` を使わない。** iOS はシステムダイアログが出ると音声セッションを持っていき、
   AudioContext が中断される。確認は盤面の中に出す
2. **`resume()` はユーザー操作の中でしか通らない。** `visibilitychange` から呼んでも弾かれる。
   復帰は三重（statechange でオーバーレイ／visibilitychange／画面のどこかを触る）
3. **意図的な停止は自動復帰の対象外にする。** これをやらないと停止ボタンを押した指で鳴り出す
4. **AudioParam の直接代入はクリック音になる。** 変更は `engine.ramp()`（setTargetAtTime）経由
5. **ボイス削除は release をかけてから disconnect**。即切りはプツッと鳴る
6. **リバーブの IR 再生成は音が途切れる。** ノブのドラッグ中は生成せず、離したときに一度だけ
7. **ノイズバッファは全ボイスで共有**。ボイスごとに作らない
8. **復帰時に GRAIN のスケジューラをリセット**。過去時刻に予約して暴発する
9. **DynamicsCompressor を省略しない。** ドローン複数本で容易にクリップする
10. **WaveShaper は `oversample: '4x'` 必須**。ディレイの feedback は 0.85 を超えさせない
11. **送りは `levelGain` より前から分岐する。** 後ろに回すと距離の表現が消える
12. **ディレイの帰還フィルタは左右別々にする。** 1本に集めると帰還がモノに潰れる。
    襷掛けなので一周の利得は片側の2乗。`setDelayFeedback()` で √ を掛けて戻してある
13. **GRAIN は密度で音量が変わらないようにする。** 間隔 0.05s × 粒 2000ms で 40粒重なる。
    `1/√(重なり数)` で割る。粒の窓は余弦。直線の三角だと短い粒で角が鳴る
14. **見た目と当たり判定を分ける。** 星は小さく描くが、掴める範囲は 46px 確保してある
15. **位置を `[0,1]` に丸めない。** 丸めると盤面の外で距離が頭打ちになり、音の変化が止まる

## 制約

- 同時発音 8点。埋まったら新規配置を断る。古い星の自動削除はしない
- 衝突判定は一切ない。星どうしは素通りし、軌道が交差しても何も起きない
- パッチは version 3。`localStorage` に debounce 500ms で保存。旧 `drift.patch.v1` から引き継ぐ
- ソロとミュートは保存しない。無音で復帰すると壊れたように見えるため
- 起動時のボイスは一斉に立ち上げない。散らして入れる（揃うと「全部同時に開いた」と聞こえる）

## 確かめ方

推測で書かず、**実際に鳴らして測る**こと。ヘッドレス Chromium が使える。

```js
// Playwright は /opt/node22/lib/node_modules/playwright
// 盤面を操作したあと、モジュールを import して中を読む
await page.evaluate(async () => {
  const { engine } = await import('/js/audio/engine.js');
  const v = engine.voices[0];
  return { gain: v.levelGain.gain.value, send: v.reverbSend.gain.value };
});
```

- 音量やリバーブは AnalyserNode か AudioParam の値を直接読む
- L/R の相関を測ればステレオの広がりが分かる
- リミッタ後のピークを測ればクリップの有無が分かる
- `page.on('pageerror')` を必ず張る。UI の不具合はたいてい例外が原因

注意: Playwright の `hasText` は部分一致なので、`'ON'` は `'DRONE'` にも当たる。

## 残っている宿題

- 盤面の左上の角をタップすると、音源ピッカーが停止ボタンの下に潜る
- Phase 2: マイク入力、グラニュラー音源、MediaRecorder による録音。
  全ボイスが masterBus に集まる構造は守ってあるので、録音は数十行で入る
